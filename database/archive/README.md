# Archived SQL

One-off or superseded schema snippets, not part of the numbered migration set in `database/*.sql` or `database/migrations/*.sql`.

- `add-corruption-prevention-constraints.sql`
- `enhance-blog-discovery-schema.sql`
- `enhanced-content-analysis-schema.sql`

Apply only if needed for a specific environment; the main schema is built from `database/` and `database/migrations/`.
