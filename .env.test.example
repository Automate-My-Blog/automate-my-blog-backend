# Copy to .env.test for local integration tests.
# CI sets these via workflow; do not commit .env.test.

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/automate_my_blog_test
USE_DATABASE=true
JWT_SECRET=test-jwt-secret-do-not-use-in-production
JWT_REFRESH_SECRET=test-refresh-secret
STRIPE_WEBHOOK_SECRET=whsec_test_secret
STRIPE_SECRET_KEY=sk_test_dummy
# Required for app to load (index.js imports OpenAI). Use a dummy value; no real calls in tests.
OPENAI_API_KEY=sk-dummy-for-tests

# Optional: disable scheduler in tests
EMAIL_SCHEDULER_ENABLED=false
