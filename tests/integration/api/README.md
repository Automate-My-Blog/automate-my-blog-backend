# API integration tests

Place endpoint-level tests here (e.g. `auth.test.js`, `generation.test.js`), per [Testing Strategy](../../../docs/reference/testing-strategy.md). Start with auth, then content generation, then API contract tests.
